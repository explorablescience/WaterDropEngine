[package]
name = "wde-examples"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
wde = { path = "../crates/wde" }
bytemuck = { version = "1.14", features = [ "derive" ] }

[dependencies.bevy]
version = "0.17"
default-features = false
features = [
    "multi_threaded",
    "bevy_asset",
    "bevy_winit",
    "x11"
]

[build-dependencies]
anyhow = "1.0"
fs_extra = "1.2"

[features]
default = []
remote = ["wde/remote"]
watch = ["wde/watch"]
trace = ["wde/trace"]
